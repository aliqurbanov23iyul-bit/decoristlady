import {sql} from '../lib/db.js'; import {requireAdmin} from '../lib/auth.js'; export default async function(req,res){try{if(req.method==='GET')return res.json(await sql`select * from reviews where active=true order by id desc`);await requireAdmin(req);const b=req.body||{};if(req.method==='POST'){const r=await sql`insert into reviews(name,text,rating,active) values(${b.name},${b.text},${+b.rating||5},true) returning *`;return res.json(r[0])}if(req.method==='DELETE'){await sql`delete from reviews where id=${+req.query.id}`;return res.json({ok:true})}res.status(405).end()}catch(e){res.status(e.message==='UNAUTHORIZED'?401:500).json({error:e.message})}}
