import {sql} from '../lib/db.js'; import {requireAdmin} from '../lib/auth.js';
export default async function(req,res){try{if(req.method==='GET')return res.json(await sql`select * from services where active=true order by sort_order,id`);await requireAdmin(req);const b=req.body||{};if(req.method==='POST'){const r=await sql`insert into services(title,description,icon,active,sort_order) values(${b.title},${b.description||''},${b.icon||'♡'},true,${+b.sort_order||0}) returning *`;return res.json(r[0])}if(req.method==='DELETE'){await sql`delete from services where id=${+req.query.id}`;return res.json({ok:true})}res.status(405).end()}catch(e){res.status(e.message==='UNAUTHORIZED'?401:500).json({error:e.message})}}
